# Croissant Shop — home-work-52

Мінімалістичний інтернет-магазин для продажу та покупки круасанів. Проект демонструє використання React та Redux Toolkit для управління станом.

----------

## Особливості

- React + Vite
- Redux Toolkit (слайси + createAsyncThunk)
- React Router
- Мінімалістичний адаптивний дизайн
- Легка інтеграція з бекендом (готові місця для підключення axios)

-----------

📂 Структура проєкту

home-work-52/ 
├── package.json 
├── vite.config.js 
├── README.md 
├── public/ 
│    └── favicon.ico
├── images/ 
│ ├── almond croissant.jpg 
│ ├── chocolate croissant.jpg 
│ └── classic croissant.jpg 
└── src/ 
├── main.jsx 
├── App.jsx 
├── index.css 
├── app/ 
│ ├── store.js 
│ └── hooks.js 
├── components/ 
│ ├── Header.jsx 
│ ├── Footer.jsx 
│ ├── Button.jsx 
│ └── ProductCard.jsx 
├── features/ 
│ ├── products/ 
│ │ ├── productsSlice.js 
│ │ └── productsApi.js 
│ └── cart/ 
│ └── cartSlice.js
├── pages/
│ ├── HomePage.jsx 
│ ├── ProductPage.jsx 
│ └── CartPage.jsx 
├── services/ 
│ └── baseApi.js 
└── utils/  
└── helpers.js

---------

## Запуск локально

1. Клонуйте репозиторій
git clone <YOUR_REPO_URL>
cd home-work-52

2. Встановіть залежності
npm install

3. Запустіть проект
npm run dev

4. Відкрийте у браузері `http://localhost:5173` (порт може бути інший)

-------------

## Деплой

- Для Vercel: підключіть репозиторій і вкажіть `npm run build` як команду збірки, `dist` як директорію для публікації.
- Для Netlify: аналогічно, вкажіть `npm run build` та `dist`.

-----------

## Інтеграція з бекендом

- Файли `src/services/baseApi.js` та `src/features/products/productsApi.js` підготовлені для заміни фейкових даних на реальні ендпоінти axios.

---------

## Критерії оцінювання

1. Ініціалізація за допомогою Vite — так.
2. React + Redux Toolkit — реалізовано.
3. Проект запускається — повинен запускатися без помилок.
4. Чистота коду — компоненти розділені, використовуються const, зрозумілі імена.
5. README.md — цей файл містить інструкції.

-----

## Подальші покращення (опційно)

- Додавання авторизації (auth slice)
- Збереження кошика в localStorage
- Підключення справжнього бекенду / REST API
- Тести (Jest + React Testing Library)

------

## Поради та контрольні кроки (як перевірити, що не має помилок)

1. Після `npm install` запустіть `npm run dev` — перевірте консоль браузера на помилки.
2. Якщо картинки не підвантажуються, перевірте URL в `productsApi.js` або підставте власні локальні зображення у `public/`.
3. Якщо щось не працює із Redux — перевірте підключення `Provider` в `main.jsx`.
4. Для лінтингу запустіть `npm run lint`.

----------
🔗 Демо-версія: https://home-work-52.netlify.app/
